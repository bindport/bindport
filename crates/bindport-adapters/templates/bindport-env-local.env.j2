# Generated by BindPort. Edit the template, not this rendered file.
BINDPORT_PROJECT={{ route.project }}
BINDPORT_SERVICE={{ route.service }}
BINDPORT_STATE={{ route.state }}
PORT={{ route.port }}
BINDPORT_HOST={{ route.host }}
BINDPORT_URL={{ route.url }}
BINDPORT_TARGET_URL={{ route.target_url }}
{% if route.hostname %}
BINDPORT_HOSTNAME={{ route.hostname }}
{% endif %}
{% if route.route_url %}
BINDPORT_ROUTE_URL={{ route.route_url }}
{% endif %}
