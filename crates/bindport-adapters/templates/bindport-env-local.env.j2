# Generated by BindPort. Edit the template, not this rendered file.
BINDPORT_PROJECT={{ route.project | dotenv }}
BINDPORT_SERVICE={{ route.service | dotenv }}
BINDPORT_STATE={{ route.state | dotenv }}
PORT={{ route.port }}
BINDPORT_HOST={{ route.host | dotenv }}
BINDPORT_URL={{ route.url | dotenv }}
BINDPORT_TARGET_URL={{ route.target_url | dotenv }}
{% if route.hostname %}
BINDPORT_HOSTNAME={{ route.hostname | dotenv }}
{% endif %}
{% if route.route_url %}
BINDPORT_ROUTE_URL={{ route.route_url | dotenv }}
{% endif %}
